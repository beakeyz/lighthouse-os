#ifndef __ANIVA_LWND_DYN_SPRITE__
#define __ANIVA_LWND_DYN_SPRITE__

/*
 * A dyn sprite is a sprite that can be rendered either before or after
 * the windows are drawn. Sprites should be small and they should obey the pixel bitmap,
 * just like windows
 */

#endif // !__ANIVA_LWND_DYN_SPRITE__

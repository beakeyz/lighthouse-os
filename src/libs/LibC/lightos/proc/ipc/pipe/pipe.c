#include "pipe.h"
#include "lightos/handle_def.h"

static HANDLE upi_handle;

static int __check_upi()
{
    return 0;
}

int init_lightos_pipe(lightos_pipe_t* pipe, const char* name)
{
    return 0;
}

#ifndef __LIGHTOS_LIBXML_H__
#define __LIGHTOS_LIBXML_H__

#define XML_START_TOKEN '<'
#define XML_END_TOKEN '>'
#define XML_CLOSE_BLOCK_TOKEN '/'
#define XML_ASSIGNMENT_TOKEN '='

/*
 * Main context where the parsed XML properties and nodes
 * live
 */
typedef struct xml_context {

} xml_context_t;

#endif // !__LIGHTOS_LIBXML_H__

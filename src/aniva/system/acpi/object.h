#ifndef __ANIVA_ACPI_OBJECT__
#define __ANIVA_ACPI_OBJECT__

/*
 * Represents a variable in the AML
 */
typedef struct {

} acpi_var_t;

/*
 * Represents an object in the AML, which can mean different things,
 * like values, references to other objects/variables, ect.
 */
typedef struct {

} acpi_object_t;

#endif // !__ANIVA_ACPI_OBJECT__

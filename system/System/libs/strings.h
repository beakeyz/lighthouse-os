#ifndef __LIGHTENV_STRINGS__
#define __LIGHTENV_STRINGS__

#include <stddef.h>

extern int strcasecmp(const char* s1, const char* s2);
extern int strncasecmp(const char* s1, const char* s2, size_t n);

#endif // !__LIGHTENV_STRINGS__
